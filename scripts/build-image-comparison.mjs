import {readFileSync,writeFileSync,existsSync} from 'node:fs';
const data=JSON.parse(readFileSync('docs/comparisons/image-2-vs-2.5.json','utf8'));
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const image=(url,label)=>`<figure><figcaption>${label}</figcaption><a href="${esc(url)}" target="_blank" rel="noopener"><img src="${esc(url)}" alt="${label}" loading="lazy"></a></figure>`;
const sections=data.cases.map(c=>`<article id="case-${c.caseId}"><h2>#${c.caseId} ${esc(c.title)}</h2><div class="pair">${image(c.baselineImage,'既有圖庫圖片')}${c.outputImage?image(c.outputImage,'Codex 內建產圖 · 2026/9/9'):'<p class="pending">待補原始參考照片，尚未重新產圖。</p>'}</div>${c.reviewNotes?`<p>${esc(c.reviewNotes)}</p>`:''}<details><summary>查看相同提示詞</summary><pre>${esc(c.promptZh)}</pre></details></article>`).join('');
for(const c of data.cases) for(const path of [c.baselineImage,c.outputImage].filter(Boolean)) if(!existsSync('data'+path)) throw new Error('Missing '+path);
writeFileSync('data/comparisons/codex-2026-09-09/index.html',`<!doctype html><html lang="zh-Hant-TW"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>圖庫產圖比較 · 2026/9/9</title><style>body{margin:0;background:#080d19;color:#edf5ff;font:17px/1.65 system-ui}main{max-width:1200px;margin:auto;padding:24px}a{color:#78e4ec}header{max-width:850px}article{border-top:1px solid #344154;margin-top:40px;padding-top:20px}.pair{display:grid;grid-template-columns:1fr 1fr;gap:20px}figure{margin:0}figcaption{padding:10px}img{width:100%;height:540px;object-fit:contain;background:#121b2b;border-radius:12px}pre{white-space:pre-wrap;overflow-wrap:anywhere;font:inherit}.pending{padding:24px;background:#121b2b}summary{cursor:pointer} @media(max-width:650px){.pair{grid-template-columns:1fr}img{height:auto}main{padding:16px}}</style></head><body><main><header><a href="/">← 返回圖庫</a><h1>相同提示詞，再產一次</h1><p>已產生 ${data.cases.filter(c=>c.outputImage).length} 張，另有 ${data.cases.filter(c=>!c.outputImage).length} 個案例待補參考圖。點圖片可查看原尺寸。</p><p>左側保留既有 GPT-Image-2 圖庫圖片，右側為 2026/9/9 的 Codex 內建產圖實測。工具未回報精確模型型號，因此不標示為已核實的 Flare 或 Sunburst。</p><p>使用同一份提示詞；部分提示詞含未填入的變數，由模型自行解讀。舊圖的原始設定未完整保留，這是視覺比較，不是受控模型評測。圖片中的文字、數據與商業資訊可能有錯誤或虛構內容。</p></header>${sections}</main></body></html>`);
